{{> header}}

<section>
  <h2>Featured Collection</h2>
  <div class="grid">
    {{#each forTradeCards}}
      {{> pokemon-card loggedIn=../loggedIn}}
    {{/each}}
    {{#each forSaleCards}}
      {{> pokemon-card  parameter=1 loggedIn=../loggedIn}}
    {{/each}}
  </div>
</section>

{{> footer}}

<script src="./js/main.js"></script>
<script src="./js/buy.js"></script>